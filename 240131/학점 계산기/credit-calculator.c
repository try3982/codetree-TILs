#include <stdio.h>

int main() {
    // 여기에 코드를 작성해주세요.
    int n;
    int avg;
    scanf("%d",&n);
    for(int i=0; i<n; i++) {
        scanf("%f",&s);
        if(s >=4.0) {
            printf("Perfect");
        }else if(s >=3.0) {
            printf("Good");
        }else{
            printf("Poor");
        }
        
    }

    return 0;
}